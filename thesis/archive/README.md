# Archive (legacy / superseded)

Older material kept for context. Start with `thesis/README.md` for the current research entrypoint.

- `paleovelocity_pilot/`: an early prototype of genus centroid “paleovelocity” + extinction association. Superseded by the bias-aware
  pipeline in `thesis/paleobiotic_velocity/`.

Most rerunnable outputs under archived folders should be written to an `output*/` directory (gitignored) to avoid overwriting tracked
snapshot artifacts.
